from langchain_anthropic import ChatAnthropic
from dotenv import load_dotenv

load_dotenv()

##modal = ChatAnthropic(modal='')

#result = modal.invoke("what is the capital of India?")

#print(result.content)